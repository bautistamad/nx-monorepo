name: CI
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: wilab/pnpm-monorepo
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: nrwl/nx-set-shas@v3
      - run: npm ci
      - run: pnpm config set store-dir .pnpm-store
      - run: NX_HEAD=$CI_COMMIT_SHA
      - run: NX_BASE=${CI_MERGE_REQUEST_DIFF_BASE_SHA:-$CI_COMMIT_BEFORE_SHA}
      - run: echo 📑 NX_HEAD=${NX_HEAD}
      - run: echo 📑 NX_BASE=${NX_BASE}
      # - run:  | # Case when first commit on a branch don't appear as a MR and has previous hash as all 0s
      #         if [ "$NX_BASE" = "0000000000000000000000000000000000000000" ]; then
      #         NX_BASE=$(git log --format="%H" -n 1 HEAD~)
      #         fi
      # - run: echo 📑 new NX_BASE=${NX_BASE}


      # - run: npx nx workspace-lint
      # - run: npx nx format:check
      # - run: npx nx affected --target=lint --parallel=3
      # - run: npx nx affected --target=test --parallel=3 --ci --code-coverage
      # - run: npx nx affected --target=build --parallel=3
